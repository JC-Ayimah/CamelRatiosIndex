% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_camel_index.R
\name{plot_camel_index}
\alias{plot_camel_index}
\alias{autoplot.camel_index}
\title{Plot CAMEL Index Percentage Differences}
\usage{
plot_camel_index(
  x,
  highlight_banks = NULL,
  add_reference_line = TRUE,
  point_size = 3,
  line_size = 0.8,
  colour_palette = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  theme_fn = ggplot2::theme_minimal,
  ...
)

\method{autoplot}{camel_index}(object, ...)
}
\arguments{
\item{x}{An object of class \code{"camel_index"} returned by \code{\link[=camel_index]{camel_index()}}.}

\item{highlight_banks}{Optional character vector of bank names to highlight
with distinct colours. All other banks are shown in grey.}

\item{add_reference_line}{Logical indicating whether to add a horizontal
reference line at PD = 0 (the base year level). Default is \code{TRUE}.}

\item{point_size}{Numeric, size of points. Default is \code{3}.}

\item{line_size}{Numeric, size of line segments. Default is \code{0.8}.}

\item{colour_palette}{Character vector of colours for highlighted banks.
Default uses a ColorBrewer qualitative palette.}

\item{title}{Optional plot title. If \code{NULL} (default), a descriptive title
is generated.}

\item{subtitle}{Optional plot subtitle.}

\item{caption}{Optional plot caption. If \code{NULL} (default), a caption
describing the base year is generated.}

\item{theme_fn}{A ggplot2 theme function. Default is \code{\link[ggplot2:theme_minimal]{ggplot2::theme_minimal()}}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_line]{ggplot2::geom_line()}} and
\code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}

\item{object}{An object of class \code{"camel_index"} (for the \code{autoplot} generic).}
}
\value{
A ggplot object.
}
\description{
Creates a ggplot2 line graph showing the percentage difference (PD) from the
base year for each bank, enabling visual comparison of bank performance
across the CAMEL framework.
}
\examples{
# Basic plot
result <- camel_index(camel_2015, camel_2022)
plot_camel_index(result)

# Highlight specific banks
plot_camel_index(result, highlight_banks = c("Absa", "Ecobank", "GCB"))

# Custom styling
plot_camel_index(
  result,
  highlight_banks = c("Absa", "Ecobank"),
  title = "Bank Performance: 2015 vs 2022",
  subtitle = "Percentage difference from base year",
  color_palette = c("#E41A1C", "#377EB8"),
  theme_fn = ggplot2::theme_bw
)

}
